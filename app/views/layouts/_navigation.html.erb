 <a class="item">
  <%= link_to "Chatroom", root_path, class:"item" %>
</a>
<a class="item">
  Messages
</a>
<div class="right menu">
  <div class="ui dropdown item">
   Account <i class="dropdown icon"></i>
  <div class="menu">
    <% if logged_in? %>
      <%= link_to "Log out", logout_path, class:"teal item", data: { turbo: true, turbo_method: :delete, turbo_confirm: 'Are you sure you want to logout?' } %>
    <% else %>
       <%= link_to "Log in", login_path, class:"teal item" %>
    <% end %>´

    </div>
    </div>
    <% if !logged_in? %>
      <div class="item">
          <div class="ui violet button">Sign Up</div>
      </div>
    <% end %>
</div>